= render "layouts/upper_menu_rfq"
.panel.panel-default
  .panel-heading
    h3.title-gray-heading
      span.glyphicon.glyphicon-list
      | &nbsp; Request For Quotation List
  .panel-body
    br
    table.table.table-hover.table-striped
      thead
        tr
          th RFQ Title
          th Principal Name
          th Duns
          th Cage Code
          th Action
      tbody.btn-icon-hover
        - @rfqs.each do |rfq|
          tr
            td= rfq.title
            td= rfq.principal.name
            td= rfq.duns
            td= rfq.cage_code
            td
              - @rfqs_items = check_rfq_items(rfq.id)
              - if @rfqs_items != 0 
                = link_to icon_text('list', ''), rfq_anchorage_billings_api_rfqs_path(rfq_id: rfq.id, step: 5), title: 'View Billing', class: 'btn btn-xs btn-info'
              
.row
  .col-xs-12
    = @logreq.vessel.name
  .col-xs-12
    = @logreq.date_of_arrival.present? ? @logreq.date_of_arrival.strftime("%B %d, %Y") : ''
    | &nbsp;-&nbsp;
    = @logreq.date_of_departure.present? ? @logreq.date_of_departure.strftime("%B %d, %Y") : ''
  .col-xs-12
    = @logreq.pier
br
h3 style="color: red;" FP LOGREQ RESPONSES
br
.row
  .col-xs-12
    = @logreq.information.html_safe
  br  
- @logreq_responses.each do |logreq_response|
  .row
    .col-xs-12
      = logreq_response.question.html_safe
  .row    
    .col-xs-12 style="color: red;"
      = logreq_response.response.html_safe
  br
.row
  .col-xs-12
    = @logreq.ending_text.html_safe